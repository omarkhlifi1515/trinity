# AgentTrinity - Agent Component (Starter)

Python-based agent skeleton that implements the agent-side automation hooks and example server functions (create_task, send_notification, update_status).

Quick start

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
python agent.py --help
```
