plugins {
    // Intentionally minimal: Android plugin will be added by Android Studio when importing.
}

// Open this folder in Android Studio. The `app` module contains the Kotlin + Compose source.
